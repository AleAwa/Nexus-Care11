// Database relations - add relations when needed
// import { relations } from "drizzle-orm/relations";
// import { users, posts } from "./schema";
// 
// export const usersRelations = relations(users, ({ many }) => ({
//   posts: many(posts),
// }));
